{
  "name": "pol-deny-prerelease-vml-module-versions",
  "properties": {
    "displayName": "Restrict Deploying Resources using VML Pre-release modules",
    "description": "VML -prerelease versions are published from branches other than the main branch where the module code has not been PR reviewed yet. VML -prerelease module versions should not be used to deploy resources in production environments.",
    "metadata": {
      "category": "Code Vulnerability",
      "version": "1.0.0",
      "preview": false,
      "deprecated": false
    },
    "mode": "Indexed",
    "parameters": {
      "effect": {
        "type": "String",
        "metadata": {
          "displayName": "Effect",
          "description": "Enable or disable the execution of the policy"
        },
        "allowedValues": [
          "Audit",
          "Deny",
          "Disabled"
        ],
        "defaultValue": "Deny"
      }
    },
    "policyRule": {
      "if": {
        "allOf": [
          {
            "field": "tags[hidden-vml_version]",
            "exists": true
          },
          {
            "field": "tags[hidden-vml_version]",
            "like": "*-prerelease"
          }
        ]
      },
      "then": {
        "effect": "[parameters('effect')]"
      }
    }
  }
}
