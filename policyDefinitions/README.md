# Sample Policy Definitions

This folder contains sample Azure Policy definition files in JSON format.

These files are automatically deployed by the Policy Definition Azure DevOps pipeline and GitHub Actions workflow.
