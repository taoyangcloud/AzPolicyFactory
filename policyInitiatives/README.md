# Sample Policy Initiatives

This folder contains sample Azure Policy initiative files in JSON format.

These files are automatically deployed by the Policy Initiative Azure DevOps pipeline and GitHub Actions workflow.
